[package]
name = "mascii-wasm"
version = "0.1.0"
edition = "2021"
description = "WASM bindings for mascii: AA math <-> AST editor and converters"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
mascii = { path = "..", default-features = false }
wasm-bindgen = "0.2"
