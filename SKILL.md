---
name: mascii-math
description: >
  Read and write mascii AA math — 2D plain-text formulas (Unicode-first
  ASCII art) that map one-to-one to a math AST and convert losslessly to
  LaTeX / Typst. Use when asked to write, edit or interpret formulas in
  mascii AA form, or to embed re-editable math in plain-text documents.
---

# mascii AA 数式の読み書き

mascii の AA 形式は「見た目が数式そのもの」でありながら、機械的に
LaTeX / Typst へ変換できるプレーンテキスト表現である。この文書だけで
正しい AA を書けるように規則をまとめる。

## 検証コマンド(利用可能なら必ず使う)

```sh
echo '<AA>' | mascii fmt      # パースして正準形に整形(エラーなら不正)
echo '<AA>' | mascii aa2tex   # LaTeX に変換して意味を確認
```

書いた AA は `mascii fmt` に通り、出力が自分の意図と一致すれば正しい。
手書きでは **寛容モード**が使える: ASCII の `x+1` はそのまま書いてよい
(正準形は数学イタリック体 `𝑥` だが、fmt が変換してくれる)。

## 基本原則

1. 数式は文字グリッド。**各部分式は矩形領域+基線行**を持ち、兄弟は
   互いに素な列範囲を占める。
2. 上下に重なる位置関係(分子/分母、極限、スクリプト)は空白でなく
   **構造グリフ**(罫線・バンド・括弧列)で範囲を示す。
3. 迷ったら1行で書く: `x²+1` のような1行表現はほぼ常に安全。

## 構造の書き方

### 分数 — 罫線 `─` (U+2500)
罫線は分子・分母より広く(最大幅+2)、罫線行が基線。
```
 1        a+b
───   ─────────
 2      c + d
```
`-`(マイナス)は `−` で描かれるので罫線と衝突しない。ASCII入力可。

### 上付き・下付き — 可能ならインライン文字
`x²` `aᵢ` `e⁻ⁱ`(対応文字: ⁰¹²³⁴⁵⁶⁷⁸⁹⁺⁻⁼⁽⁾ⁿⁱ /
₀₁₂₃₄₅₆₇₈₉₊₋₌₍₎ₐₑₕᵢⱼₖₗₘₙₒₚᵣₛₜᵤᵥₓ)。
インライン化できない内容は 2D 配置(基底の**右上/右下**に置く):
```
 α+1
𝑥        ← x^(α+1)     𝑥      ← x_(α+1)
          α+1
```
行頭にスクリプトだけを置く場合は基底 `⬚` を明示: `⬚²`。

### 累乗根 — `√ ∛ ∜` + オーバーライン `_`
```
 ____        ____
√x+12       │ 1
            │ ───    ← 茎 │ が内容の全行を覆い、最下行が根号(例は3乗根)
            ∛  2
```
オーバーラインの長さ=引数の幅。1行内容なら `√` の右に直接。

### 大型演算子の極限 — バンド `┄` (U+2504)
極限の範囲をバンドで明示し、演算子を中央に埋める。**バンドなしの
上下極限は書いてはいけない**(曖昧になる)。極限内は詰め組み(`n=1`)。
```
  ∞
┄┄∑┄┄ aₙ        ∫ f(x)dx      ← 極限なしは裸の ∫ でよい
 n=1
```

### 括弧 — 1行は `( )`、複数行は `⎛⎜⎝ ⎞⎟⎠`
```
⎛    1 ⎞
⎜1 + ─ ⎟
⎝    x ⎠
```

### 行列 — `⎡⎢⎣ ⎤⎥⎦`(1行なら `[ ]`)
列区切り=**空白2列**、行区切り=**空白1行**。基線=中央行。
```
⎡a  b⎤
⎢    ⎥
⎣c  d⎦
```

### アクセント — 基底1文字の真上/真下に1マーク
マーク文字は専用: `^`(hat) `˜`(tilde, ~とは別字) `¯`(bar)
`⇀`(vec, →とは別字) `˙`(dot) `¨`(ddot) `ˇ` `˘` `˚` / 下: `‗`(underline)
```
⇀       ⇀
E ⋅ d A      ← 例: ベクトル E, A(マークは基底の真上のセル)
```

### 打ち消し(cancel)— 各セルに U+0338 を後置
打ち消す部分の**全ての文字**の直後に結合ロングソリダス U+0338 を置く:
`y̸`。分数の約分などに:
```
 𝑥y̸
────      → \frac{x\cancel{y}}{\cancel{y}}
 y̸
```

### 関数名 — 立体 ASCII で書く
`sin cos tan log ln exp lim det …` は立体のまま書く(変数はイタリック体
`𝑥` になるので区別される)。ASCII手書きなら `sinx` は sin·x と解釈される。

## 予約文字(原子として使ってはいけない)

`─ ┄ │ √∛∜ ⎛⎜⎝⎞⎟⎠ ⎡⎢⎣⎤⎥⎦ ⬚ ▌ ▶ ^ ˜ ¯ ⇀ ˙ ¨ ˇ ˘ ˚ ‗`、
基線より上の行の `_`、数学イタリック体(𝐴–𝑧, ℎ)、インライン上付き・
下付き文字。これら以外の Unicode 数式記号(α ≤ ∈ → ℝ ⊗ …)は自由に使える。

## 曖昧さを避けるコツ

- 基線がどの行か曖昧な手書きAAでは、基線行の行頭に `▶` を置ける
  (パース時に除去される)。
- 空の上付き/下付き(`⬚` だけのスクリプト)は書かない(正準形に存在しない)。
- 2Dスクリプトの直後に別の要素が来るときは空白1列空けると読みやすい
  (正準形も同じ規則)。
- 積分・総和の極限は必ずバンド `┄` で。`∑` の上下にただ置くだけでは
  入れ子(極限の中の ∫ など)が曖昧になる。

## 例(正準形)

二次方程式の解:
```
        ________
 −𝑏 ± √𝑏² − 4𝑎𝑐
──────────────────
        2𝑎
```

ガウス積分:
```
  ∞    −𝑥²      _
┄┄∫┄┄ 𝑒    𝑑𝑥 = √π
 −∞
```

ベイズの定理(1行で十分な例):
```
𝑃(𝐴|𝐵) = 𝑃(𝐵|𝐴)𝑃(𝐴)/𝑃(𝐵)   ← ただし分数を 2D にした方が読みやすい
```

回転行列:
```
    ⎡cos𝜃  −sin𝜃⎤
R = ⎢           ⎥
    ⎣sin𝜃  cos𝜃 ⎦
```

## 完全な仕様

`docs/aa-spec.md`(正準AA形式仕様)と `docs/design.md`(設計判断)を参照。
確信が持てない構造は、まず `mascii` の TUI かライブラリ
(`mascii::render::render_row` + `RenderCtx::canonical()`)で生成した
出力を手本にすること。
