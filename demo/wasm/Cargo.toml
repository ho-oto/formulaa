[package]
name = "mascii-wasm"
version = "0.1.0"
edition = "2024"
description = "WASM bindings for mascii, as used by the self-contained web demo"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
mascii = { path = "../..", default-features = false }
wasm-bindgen = "0.2"
